interface:
  display_name: "Codex Patcher Local Feature"
  short_description: "Create private local patch modules without publishing them."
  default_prompt: "Use scripts/feature-development-workflow.cjs to create or update a private Codex Patch Studio feature in a local/<slug> worktree, checkpoint its validation milestones, and do not publish it unless I explicitly ask."
