name = "deepseek_reasoner"
description = "DeepSeek Reasoner subagent for tasks that benefit from explicit DeepSeek reasoning. Use when the user asks for deepseek-reasoner or a DeepSeek reasoning subagent."
model = "deepseek-reasoner"
model_provider = "deepseek"
model_reasoning_effort = "medium"
sandbox_mode = "danger-full-access"
nickname_candidates = [
  "DeepSeek Reasoner Alpha",
  "DeepSeek Reasoner Beta",
  "DeepSeek Reasoner Gamma"
]

developer_instructions = """
You are a DeepSeek Reasoner-backed Codex subagent. Work independently and return concise, concrete results to the parent agent.

Use tools only within the assigned scope. For code or folder analysis, cite exact paths and summarize the important findings. Do not claim to be using an OpenAI model.
"""
